# 检查上传限制的Nginx配置
# 这个配置用于诊断413错误

server {
    listen 80;
    listen [::]:80;
    server_name edupro.adddesigngroup.com;

    # 全局文件上传大小限制
    client_max_body_size 50M;
    
    # 全局超时配置
    client_body_timeout 300s;
    client_header_timeout 300s;
    
    # 上传文件服务
    location /uploads/ {
        root /opt/EduPro/backend/src;
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
        add_header Access-Control-Allow-Origin *;
    }

    # API 代理 - 专门处理上传
    location /api/uploads/ {
        # 专门为上传API设置更大的限制
        client_max_body_size 50M;
        client_body_timeout 300s;
        
        proxy_pass http://127.0.0.1:5001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # 大文件上传超时配置
        proxy_connect_timeout 300s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;
        proxy_request_buffering off;
        
        # 添加调试头
        add_header X-Debug-Upload-Limit "50M" always;
        add_header X-Debug-Client-Max-Body-Size $client_max_body_size always;
    }

    # 其他API代理
    location /api/ {
        proxy_pass http://127.0.0.1:5001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 其他请求重定向到HTTPS
    location / {
        return 301 https://$server_name$request_uri;
    }
}

# HTTPS配置
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name edupro.adddesigngroup.com;

    # 全局文件上传大小限制
    client_max_body_size 50M;
    
    # 全局超时配置
    client_body_timeout 300s;
    client_header_timeout 300s;



    # 上传文件服务
    location /uploads/ {
        root /opt/EduPro/backend/src;
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
        add_header Access-Control-Allow-Origin *;
    }

    # API 代理 - 专门处理上传
    location /api/uploads/ {
        # 专门为上传API设置更大的限制
        client_max_body_size 50M;
        client_body_timeout 300s;
        
        proxy_pass http://127.0.0.1:5001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # 大文件上传超时配置
        proxy_connect_timeout 300s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;
        proxy_request_buffering off;
        
        # 添加调试头
        add_header X-Debug-Upload-Limit "50M" always;
        add_header X-Debug-Client-Max-Body-Size $client_max_body_size always;
    }

    # 其他API代理
    location /api/ {
        proxy_pass http://127.0.0.1:5001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 前端静态文件
    root /opt/EduPro/frontend/dist;
    index index.html;

    # 前端路由
    location / {
        try_files $uri $uri/ /index.html;
    }
}
